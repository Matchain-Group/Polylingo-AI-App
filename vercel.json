{
  "version": 2,
  "buildCommand": "pnpm -r --if-present run build",
  "installCommand": "pnpm install",
  "framework": null,
  "build": {
    "env": {
      "PORT": "3000",
      "NODE_ENV": "production"
    }
  },
  "env": {
    "PORT": "3000",
    "NODE_ENV": "production"
  }
}
